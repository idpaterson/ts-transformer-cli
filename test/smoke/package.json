{
  "private": true,
  "name": "smoke-cli",
  "description": "",
  "dependencies": {
    "ts-transformer-cli": "file:../.."
  },
  "scripts": {
    "setup": "npm link ../..",
    "test": "tstc && tstc -p tsconfig-include-exclude.json"
  }
}